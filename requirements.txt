# Install into a virtualenv:  pip install -r requirements.txt
pipecat-ai[sarvam,anthropic,webrtc]
python-dotenv
sarvamai

# Dev runner (serves the WebRTC signaling + prebuilt client that bot.py uses)
fastapi
uvicorn
pipecat-ai-prebuilt
